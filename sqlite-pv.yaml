apiVersion: v1
kind: PersistentVolume
metadata:
  name: farminsight-backend-sqlite-pv
  namespace: farminsight
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: local-storage
  local:
    path: /mnt/rclone-tucloud-etce-server-storage/Applications/Farminsight/Backend/Database
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values:
            - etce-cluster-0
